class Solution {
    public char findTheDifference(String s, String t) {
        char result = 0;
        for (char tChar : t.toCharArray()) result += tChar;
        for (char sChar : s.toCharArray()) result -= sChar;
        return result;
    }
}
